name: analytics

on:
  workflow_dispatch:
    inputs:
      days:
        description: "集計対象の日数(start-date/end-date未指定時のみ使用)"
        required: false
        default: "28"
      start_date:
        description: "集計開始日(YYYY-MM-DD。指定した場合はdaysより優先)"
        required: false
        default: ""
      end_date:
        description: "集計終了日(YYYY-MM-DD。空欄なら今日)"
        required: false
        default: ""
      by_week:
        description: "trueなら投稿週(月曜始まり)別の1日あたり再生数の推移も出力する(「飽き」傾向の確認用)"
        required: false
        default: "false"
        type: boolean
      by_voice:
        description: "trueなら読み上げ言語別の1日あたり再生数・視聴維持率も出力する(--voice randomの比率調整の参考用)"
        required: false
        default: "false"
        type: boolean

jobs:
  analytics:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Summarize views/retention by mode
        env:
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          # yt-analytics.readonlyスコープを含めて再発行したリフレッシュ
          # トークンが必要(README「モード別パフォーマンス集計」参照)。
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
        run: |
          args=()
          if [ -n "${{ inputs.start_date }}" ]; then
            args+=(--start-date "${{ inputs.start_date }}")
          fi
          if [ -n "${{ inputs.end_date }}" ]; then
            args+=(--end-date "${{ inputs.end_date }}")
          fi
          if [ -z "${{ inputs.start_date }}" ]; then
            args+=(--days "${{ inputs.days }}")
          fi
          if [ "${{ inputs.by_week }}" = "true" ]; then
            args+=(--by-week)
          fi
          if [ "${{ inputs.by_voice }}" = "true" ]; then
            args+=(--by-voice)
          fi
          python3 youtube_analytics.py "${args[@]}"
